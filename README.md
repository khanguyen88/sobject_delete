Simple CLI to read sObject definitions and generate delete statements based on their dependencies

# How to use
`cargo run -- -d -a "delete [SELECT Id FROM {}];" "<path to sobject folder>"`

# License
MIT
